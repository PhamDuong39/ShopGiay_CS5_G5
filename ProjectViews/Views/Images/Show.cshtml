@model IEnumerable<Data.Models.Images>

@{
    ViewData["Title"] = "Show";
}
@if (Model.Count() == 0)
{
    <h1>There are no images  <a asp-action="Create">Create Now?</a></h1>
}
else
{
    <h1>Manager</h1>
    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.IdShoeDetail)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ImageSource)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.IdShoeDetail)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ImageSource)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

